{
  "GHSA-frvp-7c67-39w9": {
    "projects": ["agent-tools"],
    "expires": "2026-08-22",
    "reason": "chrome-devtools-axi depends on @modelcontextprotocol/sdk, which resolves vulnerable @hono/node-server 1.19.14; no compatible patched release is available.",
    "compensatingControl": "The advisory's encoded-backslash path traversal affects Windows only. These dotfiles deploy on macOS, and dependency CI runs on Linux."
  },
  "GHSA-mh99-v99m-4gvg": {
    "projects": ["agent-tools"],
    "expires": "2026-08-22",
    "reason": "Pi's published npm-shrinkwrap pins brace-expansion 5.0.7, so the root project cannot apply the available 5.0.8 patch until Pi republishes its dependency tree.",
    "compensatingControl": "The vulnerable expansion is reached through glob patterns from user settings, installed package manifests, or trusted project configuration; this local CLI does not expose those patterns to unauthenticated network input."
  }
}
